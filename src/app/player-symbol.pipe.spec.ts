import { PlayerSymbolPipe } from './player-symbol.pipe';

describe('PlayerSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
